server:
  port: 8082

spring:
  application:
    name: USER-SERVICE

  datasource:
    url: jdbc:postgresql://localhost:5432/zomato
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: none

      show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka